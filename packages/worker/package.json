{
  "name": "worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "build": "wrangler deploy --dry-run",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply brp-platform --local"
  },
  "dependencies": {
    "@brp/types": "0.1.0",
    "@brp/brp-api": "0.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260408.0",
    "typescript": "~6.0.2",
    "wrangler": "^4.11.1"
  }
}
